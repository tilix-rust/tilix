# Task Execution Checklist: Phase 2 — Sessions, Input Sync & Profiles Foundation

- **Task ID:** `TASK-0002`
- **Spec Reference:** `docs/specs/spec-0002-sessions-sync-profiles-2026-09-15.md`
- **Living Architecture Reference:** `docs/architecture/overview.md`
- **Workflow Level:** Medium/Large
- **Status:** Completed & Verified

---

## Allowed File Manifest

### Allowed New Files
- `src/model/theme.rs` — Pure Rust `ColorScheme`, `RgbColor`, hex conversions, and JSON parsing.
- `src/model/profile.rs` — Pure Rust `Profile`, cursor and font preferences.
- `src/model/template.rs` — Pure Rust `SessionLayoutTemplate` JSON serialization and ID remapping.
- `tests/test_phase2_domain.rs` — Integration and unit test suite for themes, templates, and split ratios.

### Allowed Modified Files
- `src/model/mod.rs` — Export new domain modules (`theme`, `profile`, `template`).
- `src/model/layout.rs` — Add `SplitId`, update `LayoutNode::Split`, add `set_split_ratio`, update existing tests.
- `src/model/session.rs` — Add session sync toggle and pane sync override tracking.
- `src/ui/mod.rs` — Export updated UI components.
- `src/ui/terminal_pane.rs` — Add sync override button, VTE `commit` hook, `feed_child`, and `apply_color_scheme`.
- `src/ui/session_view.rs` — Paned `notify::position` ratio sync, input broadcast coordination, profile application.
- `src/ui/window.rs` — Integrate `AdwTabView`, `AdwTabBar`, sync toggle button, and tab/sync accelerators.
- `docs/architecture/overview.md` — Incorporate Phase 2 living architecture updates.

### Forbidden Files
- Modifying files outside `/playground/tilix`.
- Overwriting historical records (`result-0001.md`, `task-0001.md`, `spec-0001.md`).
- Adding unapproved third-party dependencies to `Cargo.toml`.

---

## Sequenced Subtasks

### 1. Addressable Split Nodes & Ratio Updates in LayoutTree (`TASK-0002-1`)
- **Step 1 (TDD Test-First):** In `src/model/layout.rs`, write failing tests:
  - `test_split_assigns_unique_split_ids`
  - `test_set_split_ratio_updates_target_node`
  - `test_set_split_ratio_clamps_values`
  - `test_remap_ids_renumbers_panes_and_splits`
- **Step 2 (Implementation):**
  - Define `pub struct SplitId(pub u64);`
  - Update `LayoutNode::Split { id: SplitId, orientation, ratio, first, second }`.
  - Implement `next_split_id()` and `set_split_ratio(split_id, ratio)` on `LayoutTree`.
  - Update Phase 1 layout tests to match new `Split` signature.
- **Validation:** `cargo test --lib model::layout` passes 100%.

### 2. Color Schemes & JSON Palette Parsing (`TASK-0002-2`)
- **Step 1 (TDD Test-First):** Create `src/model/theme.rs` with failing tests:
  - `test_rgb_color_from_hex_valid`
  - `test_rgb_color_from_hex_invalid`
  - `test_color_scheme_parse_tilix_json`
  - `test_builtin_color_schemes`
- **Step 2 (Implementation):**
  - Implement `RgbColor` with `#RRGGBB` and `#AARRGGBB` hex parsing.
  - Implement `ColorScheme` with serde support for Tilix/GNOME palette JSON (`foreground-color`, `background-color`, `palette`).
  - Provide built-in themes: `tilix_dark()`, `tilix_light()`, `solarized_dark()`, `monokai()`.
  - Export in `src/model/mod.rs`.
- **Validation:** `cargo test --lib model::theme` passes 100%.

### 3. Profiles & Session Layout Templates (`TASK-0002-3`)
- **Step 1 (TDD Test-First):** Create `src/model/profile.rs` and `src/model/template.rs` with failing tests:
  - `test_profile_default_settings`
  - `test_template_serialize_and_deserialize`
  - `test_template_instantiate_session_remaps_ids`
- **Step 2 (Implementation):**
  - Implement `Profile`, `CursorShapePreference`, and `CursorBlinkPreference` in `src/model/profile.rs`.
  - Implement `SessionLayoutTemplate` in `src/model/template.rs` with `from_session` and `instantiate_session`.
  - Export in `src/model/mod.rs`.
- **Validation:** `cargo test --lib model::template` passes 100%.

### 4. TerminalPane Sync Controls & VTE Theming (`TASK-0002-4`)
- In `src/ui/terminal_pane.rs`:
  - Add sync override toggle button (`input-keyboard-symbolic`) to pane header.
  - Track `is_sync_enabled` flag (default `true`).
  - Connect `terminal.connect_commit` to notify parent `SessionView` of committed text.
  - Expose `feed_child(&self, data: &[u8])` to write broadcast bytes to PTY.
  - Implement `apply_color_scheme(&self, scheme: &ColorScheme)` translating to `gdk::RGBA` and calling `vte.set_colors()` and `vte.set_color_cursor()`.
- **Validation:** `cargo check --lib` succeeds.

### 5. SessionView Ratio Tracking & Input Broadcasting (`TASK-0002-5`)
- In `src/ui/session_view.rs`:
  - Store `sync_input_enabled: bool` on `SessionModel` / `SessionView`.
  - Wire `gtk::Paned`'s `notify::position` signal using `SplitId` to update `LayoutTree` ratio dynamically.
  - Wire `TerminalPane::connect_commit` into `broadcast_input`:
    - If `session.sync_input_enabled` and sender pane has sync enabled, iterate sibling panes and invoke `feed_child` on every pane with sync enabled.
  - Add title change callback forwarder so `SessionView` can notify external listeners (e.g. window tab bar) when the active pane's title changes.
- **Validation:** `cargo check --lib` succeeds.

### 6. Multi-Session Tab Management with AdwTabView in TilixWindow (`TASK-0002-6`)
- In `src/ui/window.rs`:
  - Replace single `SessionView` child with `adw::TabView` and `adw::TabBar`.
  - Add `AdwTabBar` to `adw::ToolbarView::add_top_bar` (`autohide = true`).
  - Implement tab management:
    - `add_session_tab(session_view) -> adw::TabPage`
    - Bind active pane title in each session to `tab_page.set_title()`.
    - Handle `tab_view.connect_close_page` to cleanly teardown session and close window when last tab is closed.
  - Add header bar toggle button for input sync (`network-transmit-receive-symbolic`).
  - Register window actions and accelerators:
    - `win.new-tab` -> `<Primary><Shift>t`
    - `win.close-tab` / `win.close-pane` -> `<Primary><Shift>w`
    - `win.tab-next` -> `<Primary>Page_Down`
    - `win.tab-prev` -> `<Primary>Page_Up`
    - `win.switch-tab-1` through `win.switch-tab-9` -> `<Alt>1`..`<Alt>9`
    - `win.toggle-sync-input` -> `<Primary><Shift>i`
- **Validation:** `cargo check --all-targets` succeeds.

### 7. End-to-End Build & Verification (`TASK-0002-7`)
- Execute full test suite:
  ```bash
  cargo check --all-targets
  cargo test
  cargo clippy --all-targets -- -D warnings
  cargo build
  cargo build --release
  ```
- Verify zero warnings, zero clippy violations, and 100% passing tests.
