# Task Execution Checklist: Phase 6 — Terminal Pane Title & Toolbar Visibility Controls

- **Task ID:** `TASK-0006`
- **Spec Reference:** [`docs/specs/spec-0006-pane-toolbar-terminal-title-2026-09-17.md`](file:///playground/tilix/docs/specs/spec-0006-pane-toolbar-terminal-title-2026-09-17.md)
- **Living Architecture Reference:** [`docs/architecture/overview.md`](file:///playground/tilix/docs/architecture/overview.md)
- **Workflow Level:** Medium/Large
- **Status:** Draft / Ready for Execution

---

## Allowed File Manifest

### Allowed Modified Files
- `src/model/config.rs` — Define `PaneTitleStyle` enum, extend `AppConfig` with `pane_title_style` and `pane_title_show_when_single`, update defaults, and add headless unit tests.
- `src/model/mod.rs` — Re-export `PaneTitleStyle` from `config` module.
- `src/ui/terminal_pane.rs` — Implement `set_header_visible` and `is_header_visible` on `TerminalPane`, and add unit test.
- `src/ui/session_view.rs` — Store `pane_title_style` and `pane_title_show_when_single` in `SessionView`, implement `update_pane_headers_visibility` and `set_pane_title_settings`, integrate into projection lifecycle, and add unit tests.
- `src/ui/window.rs` — Implement `apply_pane_title_settings_to_all_sessions`, and add unit test.
- `src/ui/preferences.rs` — Add "Terminal Title" preferences group in Appearance tab with `ComboRow` for "Title Style" and `SwitchRow` for "Show title when single terminal", connecting signals to `sync_and_save`.
- `docs/architecture/overview.md` — Update living architecture document to reflect Phase 6 terminal pane title and toolbar visibility controls.

### Allowed New Files
- `tests/test_phase6_pane_toolbar.rs` — Headless integration test suite verifying config serialization, legacy JSON deserialization, and reactive projection helpers.

### Forbidden Files
- Modifying production files outside the allowed list.
- Modifying or overwriting historical documents:
  - `docs/specs/spec-0001-rust-tilix-foundation-2026-09-15.md` through `docs/specs/spec-0004-packaging-distribution-polish-2026-09-16.md`
  - `docs/tasks/task-0001.md` through `docs/tasks/task-0004.md`
  - `docs/results/result-0001.md` through `docs/results/result-0005.md`
- Adding unapproved third-party dependencies to `Cargo.toml`.

---

## Sequenced Subtasks

### 1. Domain Model Extension: `PaneTitleStyle` & `pane_title_show_when_single` (`TASK-0006-1`)
- **Step 1 (TDD Test-First):** In `src/model/config.rs`, write failing unit tests:
  - `test_pane_title_style_default_is_normal`: Verifies `PaneTitleStyle::default() == PaneTitleStyle::Normal`.
  - `test_pane_title_style_serde_serialization`: Verifies `"normal"` and `"none"` JSON serialization.
  - `test_app_config_with_pane_title_defaults`: Verifies `AppConfig::default().pane_title_style == PaneTitleStyle::Normal` and `AppConfig::default().pane_title_show_when_single == true`.
  - `test_app_config_deserialize_legacy_v5_json`: Verifies JSON without Phase 6 fields populates default values without error.
  - `test_app_config_roundtrip_with_pane_title_settings`: Verifies JSON roundtrip with custom values (`PaneTitleStyle::None`, `pane_title_show_when_single: false`).
- **Step 2 (Implementation):**
  - Define `PaneTitleStyle` enum with `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)]` and `#[serde(rename_all = "snake_case")]` in `src/model/config.rs`.
  - Add fields `pub pane_title_style: PaneTitleStyle` and `pub pane_title_show_when_single: bool` to `AppConfig`.
  - Set defaults in `impl Default for AppConfig`: `pane_title_style: PaneTitleStyle::Normal`, `pane_title_show_when_single: true`.
  - In `src/model/mod.rs`, re-export `PaneTitleStyle`.
- **Validation:** `cargo test --lib model::config` passes 100%.

### 2. `TerminalPane` Header Visibility API (`TASK-0006-2`)
- **Step 1 (TDD Test-First):** In `src/ui/terminal_pane.rs` tests:
  - Add unit test `test_terminal_pane_header_visibility` (guarded by `if gtk::init().is_err() { return; }`): Creates a terminal pane, checks initial header visibility, calls `pane.set_header_visible(false)`, asserts `!pane.is_header_visible()`, calls `pane.set_header_visible(true)`, asserts `pane.is_header_visible()`.
- **Step 2 (Implementation):**
  - Add public methods `pub fn set_header_visible(&self, visible: bool)` and `pub fn is_header_visible(&self) -> bool` to `TerminalPane` in `src/ui/terminal_pane.rs`.
- **Validation:** `cargo test --lib ui::terminal_pane` passes.

### 3. Reactive Projection: Pane Toolbar in `SessionView` (`TASK-0006-3`)
- **Step 1 (TDD Test-First):** In `src/ui/session_view.rs` tests:
  - Add unit tests (guarded by `if gtk::init().is_err() { return; }`):
    - `test_session_view_pane_title_none_hides_all_headers`: Verifies setting `PaneTitleStyle::None` hides headers across all panes regardless of pane count.
    - `test_session_view_show_when_single_false_dynamic_split`: Verifies that with `show_when_single: false`, a single pane has its header hidden, but upon `split_active`, both panes automatically show their headers; upon closing the split, the remaining pane automatically hides its header.
- **Step 2 (Implementation):**
  - Add `pane_title_style: Rc<RefCell<PaneTitleStyle>>` and `pane_title_show_when_single: Rc<RefCell<bool>>` fields to `SessionView`.
  - In `SessionView::with_model_and_dir`: Initialize from `AppConfig::load()`.
  - Implement helper `pub fn update_pane_headers_visibility(&self)` in `SessionView`.
  - Call `update_pane_headers_visibility` in `rebuild_projection_with` and `reset`.
  - Implement public method `pub fn set_pane_title_settings(&self, style: PaneTitleStyle, show_when_single: bool)` on `SessionView`.
- **Validation:** `cargo test --lib ui::session_view` passes.

### 4. Reactive Projection: Global Broadcast in `src/ui/window.rs` (`TASK-0006-4`)
- **Step 1 (TDD Test-First):** In `src/ui/window.rs` tests:
  - Add unit test verifying that `apply_pane_title_settings_to_all_sessions(PaneTitleStyle::None, false)` updates registered sessions without error.
- **Step 2 (Implementation):**
  - Implement public function `pub fn apply_pane_title_settings_to_all_sessions(style: PaneTitleStyle, show_when_single: bool)` in `src/ui/window.rs`, iterating through all sessions in `WIDGET_TO_SESSION`.
- **Validation:** `cargo test --lib ui::window` passes.

### 5. Preferences UI Controls in `TilixPreferencesWindow` (`TASK-0006-5`)
- **Step 1 (Implementation):**
  - In `src/ui/preferences.rs`, on `page` ("Appearance"), add a new `adw::PreferencesGroup` titled `"Terminal Title"`.
  - Create `adw::ComboRow` for "Title Style" with model `["Normal", "None"]`.
  - Set initial selection based on `current_config.borrow().pane_title_style` (0 for `Normal`, 1 for `None`).
  - Create `adw::SwitchRow` for "Show title when single terminal" with subtitle `"Display terminal pane header bar even when no splits exist"`.
  - Set initial state based on `current_config.borrow().pane_title_show_when_single`.
  - Add both rows to `title_group`, and add `title_group` to `page`.
  - In `sync_and_save`:
    - Read selected title style and switch state.
    - Update `cfg.pane_title_style` and `cfg.pane_title_show_when_single`.
    - Save configuration via `cfg.save()`.
    - Dispatch `crate::ui::window::apply_pane_title_settings_to_all_sessions(cfg.pane_title_style, cfg.pane_title_show_when_single)`.
  - Connect `connect_selected_notify` and `connect_active_notify` signals to `sync_and_save`.
- **Validation:** `cargo check --all-targets` passes with 0 warnings.

### 6. Integration Test Suite & Living Architecture Update (`TASK-0006-6`)
- **Step 1 (Implementation):**
  - Create `tests/test_phase6_pane_toolbar.rs`.
  - Implement test cases covering:
    - Legacy v5 JSON deserialization without Phase 6 keys.
    - Full roundtrip serialization with custom title settings.
    - Visibility state calculation logic across pane counts.
    - Forward compatibility with unknown future JSON fields.
- **Step 2 (Implementation):**
  - Update `docs/architecture/overview.md` to version 0.6.0.
  - Detail `PaneTitleStyle` and `pane_title_show_when_single` reactive projection pathways and Section 17.
- **Validation:**
  - `cargo test` passes 100%.
  - `cargo clippy --all-targets -- -D warnings` passes with 0 warnings.

---

## Validation & Quality Gates

1. **Unit & Integration Test Suite:**
   - Execute `cargo test`.
   - All tests across `src/`, `tests/test_phase2_domain.rs`, `tests/test_phase3_domain.rs`, `tests/test_phase4_packaging_polish.rs`, `tests/test_phase5_window_style_wide_handle.rs`, and `tests/test_phase6_pane_toolbar.rs` must pass 100% headlessly.
2. **Compiler & Linter:**
   - Execute `cargo check --all-targets` (must produce 0 errors, 0 warnings).
   - Execute `cargo clippy --all-targets -- -D warnings` (must produce 0 violations).
3. **Backwards Compatibility:**
   - Existing user configuration files with missing keys must continue to parse cleanly without panic or data corruption.
