# Task Execution Checklist: Phase 10 — Profile Preferences UI Parity with Original Tilix

- **Task ID:** `TASK-0010`
- **Spec Reference:** [`docs/specs/spec-0010-profile-preferences-ui-parity-2026-09-17.md`](file:///playground/tilix/docs/specs/spec-0010-profile-preferences-ui-parity-2026-09-17.md)
- **Living Architecture Reference:** [`docs/architecture/overview.md`](file:///playground/tilix/docs/architecture/overview.md)
- **Workflow Level:** Medium/Large
- **Status:** Completed

---

## Allowed File Manifest

### Allowed Modified Files
- `src/ui/preferences.rs` — Complete Profile Preferences page overhaul to 100% visual parity with original Tilix screenshots.
- `docs/architecture/overview.md` — Bump architecture version to 0.10.0 and document Profile Preferences UI parity subsystem.

### Allowed New Files
- `docs/specs/spec-0010-profile-preferences-ui-parity-2026-09-17.md` — Complete architecture specification.
- `docs/tasks/task-0010.md` — Executable task checklist.
- `tests/test_phase10_profile_ui_parity.rs` — Integration test suite for Phase 10 covering widget hierarchies, tab structure, sensitivity linkages, and profile CRUD synchronization.

### Forbidden Files
- Modifying production files outside the allowed list (e.g. `src/model/*`, `src/pty/*`, `src/ui/terminal_pane.rs`, `src/ui/session_view.rs`).
- Modifying or overwriting historical documents:
  - `docs/specs/spec-0001-rust-tilix-foundation-2026-09-15.md` through `docs/specs/spec-0009-profile-customization-complete-2026-09-17.md`
  - `docs/tasks/task-0001.md` through `docs/tasks/task-0009.md`
  - `docs/results/result-0001.md` through `docs/results/result-0009.md`
- Adding unapproved third-party dependencies to `Cargo.toml`.

---

## Sequenced Subtasks

### 1. [x] TDD Test-First Framework for Phase 10 UI Parity (`TASK-0010-1`)
- **Target Files:** `tests/test_phase10_profile_ui_parity.rs`
- **Step 1 (TDD Test-First):** Create integration test suite `tests/test_phase10_profile_ui_parity.rs`:
  - `test_phase10_notebook_has_seven_canonical_tabs`: Inspects `TilixPreferencesWindow` Profiles page to verify it contains a `gtk::Notebook` with exactly 7 tabs (`General`, `Command`, `Color`, `Scrolling`, `Compatibility`, `Badge`, `Advanced`).
  - `test_phase10_profile_management_header_bar`: Verifies the top profile bar contains a `Profile:` label, a `gtk::DropDown` of profile names, and buttons `New`, `Duplicate`, `Delete`, and `Set as Default`.
  - `test_phase10_general_tab_resets`: Verifies clicking `Terminal size` Reset button sets columns to 80 and rows to 24; clicking `Cell spacing` Reset button sets width to 1.0 and height to 1.0.
  - `test_phase10_command_custom_command_sensitivity`: Verifies custom command entry sensitivity toggles based on the `Run a custom command instead of my shell` CheckButton.
  - `test_phase10_color_palette_grid_and_sliders`: Verifies the color palette contains 18 color buttons and changing any button updates the profile scheme and marks scheme as `"Custom"`; verifies transparency and unfocused dimming use `gtk::Scale`.
  - `test_phase10_scrolling_limit_sensitivity`: Verifies scrollback spin button sensitivity is coupled to `Limit scrollback to:` CheckButton.
  - `test_phase10_no_adw_rows_in_profile_page`: Verifies that no `adw::ActionRow`, `adw::SwitchRow`, or `adw::SpinRow` are used inside the Profile Preferences page.
- **Validation:** `cargo test --test test_phase10_profile_ui_parity` fails as expected before implementation.

---

### 2. [x] Profile Management Header Bar & Notebook Tab Structure (`TASK-0010-2`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** In `TilixPreferencesWindow::new`, replace the Libadwaita `PreferencesGroup` "Profile Selection and Management" and "Settings Section" dropdown with:
  - Top Profile Header Bar:
    - Horizontal `gtk::Box` (spacing 8, margin 12).
    - `gtk::Label` with text `"Profile:"` (bold).
    - `gtk::DropDown` populated with current profile names.
    - `[ New ]` `gtk::Button`.
    - `[ Duplicate ]` `gtk::Button`.
    - `[ Delete ]` `gtk::Button` (styled `.destructive-action`, disabled if `profiles.len() <= 1`).
    - `[ Set as Default ]` `gtk::Button` (disabled if active profile is default).
  - Main Tab Container:
    - `gtk::Notebook` directly under the profile header bar.
    - Append 7 pages with labels: `"General"`, `"Command"`, `"Color"`, `"Scrolling"`, `"Compatibility"`, `"Badge"`, `"Advanced"`.
- **Validation:** Notebook tab switching and profile bar buttons construct and wire cleanly.

---

### 3. [x] General Tab Implementation (`TASK-0010-3`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** Build the General tab layout using `gtk::Grid` with `column_spacing = 12`, `row_spacing = 8`, matching `screenshots/General.png`:
  - Column 0 labels right-aligned (`halign = Align::End`, `xalign = 1.0`).
  - `Profile name`: `gtk::Entry`.
  - `Terminal title`: Horizontal `gtk::Box` with `gtk::Entry` and `gtk::MenuButton` (icon `pan-down-symbolic`) with token presets popover (`${id}: ${title}`, `${title}`, `${profile}`, `${directory}`, `${appName}`).
  - Section Header `Text Appearance`: Bold label, left-aligned, top margin.
  - `Terminal size`: Horizontal `gtk::Box` with columns `gtk::SpinButton`, `"columns"` label, rows `gtk::SpinButton`, `"rows"` label, and `[ Reset ]` button (sets 80x24).
  - `Cell spacing`: Horizontal `gtk::Box` with width `gtk::SpinButton`, `"width"` label, height `gtk::SpinButton`, `"height"` label, and `[ Reset ]` button (sets 1.0x1.0).
  - `Margin`: `gtk::SpinButton` (0..500).
  - `Text blink mode`: `gtk::DropDown` (`Never`, `Focused`, `Unfocused`, `Always`).
  - `Custom font`: Horizontal `gtk::Box` with `gtk::CheckButton` and `gtk::FontButton` (sensitive only when checkbox is checked).
  - `Word-wise select chars`: `gtk::Entry`.
  - Section Header `Cursor`: Bold label, left-aligned.
  - `Cursor`: `gtk::DropDown` (`Block`, `I-Beam`, `Underline`).
  - `Cursor blink mode`: `gtk::DropDown` (`System`, `On`, `Off`).
  - Section Header `Notification`: Bold label, left-aligned.
  - `Terminal bell`: `gtk::DropDown` (`None`, `Sound`, `Icon`, `Icon and sound`).
- **Validation:** Verify layout, token insertion, reset button handlers, and font button sensitivity.

---

### 4. [x] Command Tab Implementation (`TASK-0010-4`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** Build the Command tab layout matching `screenshots/Command.png`:
  - `gtk::Box` (orientation Vertical, spacing 10, margins 16).
  - `[ ] Run command as a login shell`: `gtk::CheckButton`.
  - `[ ] Run a custom command instead of my shell`: `gtk::CheckButton`.
  - Sub-indented row (`margin_start = 24`, spacing 12):
    - `gtk::Label` `"Command"`.
    - `gtk::Entry` (custom command, sensitive only when custom command checkbox is checked).
  - Row (spacing 12):
    - `gtk::Label` `"When command exits"`.
    - `gtk::DropDown` (`Exit the terminal`, `Restart the command`, `Hold the terminal open`).
- **Validation:** Verify custom command entry dynamically disables/enables with checkbox toggle.

---

### 5. [x] Color Tab Implementation (`TASK-0010-5`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** Build the Color tab layout matching `screenshots/Color.png`:
  - Top row: `gtk::Label` `"Color scheme"` + `gtk::DropDown` (`Tilix Dark`, `Tilix Light`, `Solarized Dark`, `Monokai`, `Custom`) + `[ Export ]` `gtk::Button`.
  - Color Palette Grid:
    - 2-column `gtk::Grid`:
      - Left column:
        - `[ColorButton]` label `"Background"`
        - `[ColorButton]` `[ColorButton]` label `"Black"` (colors 0 and 8)
        - `[ColorButton]` `[ColorButton]` label `"Red"` (colors 1 and 9)
        - `[ColorButton]` `[ColorButton]` label `"Green"` (colors 2 and 10)
        - `[ColorButton]` `[ColorButton]` label `"Orange"` (colors 3 and 11)
      - Right column:
        - `[ColorButton]` label `"Foreground"`
        - `[ColorButton]` `[ColorButton]` label `"Blue"` (colors 4 and 12)
        - `[ColorButton]` `[ColorButton]` label `"Purple"` (colors 5 and 13)
        - `[ColorButton]` `[ColorButton]` label `"Turquoise"` (colors 6 and 14)
        - `[ColorButton]` `[ColorButton]` label `"Grey"` (colors 7 and 15)
  - Options Section:
    - `[ ] Use theme colors for foreground/background`: `gtk::CheckButton` + `[ Advanced v ]` `gtk::MenuButton`.
    - Advanced Popover containing:
      - Override Bold Color: `gtk::CheckButton` + `gtk::ColorButton`
      - Override Cursor Colors: `gtk::CheckButton` + BG `gtk::ColorButton` + FG `gtk::ColorButton`
      - Override Highlight Colors: `gtk::CheckButton` + BG `gtk::ColorButton` + FG `gtk::ColorButton`
    - `[x] Show bold text in bright colors`: `gtk::CheckButton`.
    - `Transparency`: right-aligned `gtk::Label` + `gtk::Scale` (horizontal slider, 0..100).
    - `Unfocused dim`: right-aligned `gtk::Label` + `gtk::Scale` (horizontal slider, 0..100).
  - Palette color modification handler: Modifying any color button writes the new color to the profile's scheme, sets the scheme name to `"Custom"`, and selects `"Custom"` in the dropdown.
- **Validation:** Verify 18 color buttons update domain colors; verify sliders update transparency and dimming percentages; verify advanced popover works.

---

### 6. [x] Scrolling Tab Implementation (`TASK-0010-6`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** Build the Scrolling tab layout matching `screenshots/Scrolling.png`:
  - `gtk::Box` (orientation Vertical, spacing 10, margins 16).
  - `[ ] Show scrollbar`: `gtk::CheckButton`.
  - `[ ] Scroll on output`: `gtk::CheckButton`.
  - `[x] Scroll on keystroke`: `gtk::CheckButton`.
  - Horizontal `gtk::Box` (spacing 10):
    - `[x] Limit scrollback to:` `gtk::CheckButton`.
    - `gtk::SpinButton` (100..100000, default 8192).
  - Wire mutual sensitivity: SpinButton is sensitive if and only if `Limit scrollback to:` is checked. Unchecked means `scrollback_unlimited = true`.
- **Validation:** Verify checkbox toggling enables/disables spin button and correctly synchronizes `scrollback_unlimited` and `scrollback_lines`.

---

### 7. [x] Compatibility & Badge Tabs Implementation (`TASK-0010-7`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1 (Compatibility Tab):**
  - Grid with right-aligned labels matching `screenshots/Compatibility.png`:
    - `Backspace key generates`: `gtk::DropDown` (`Automatic`, `Control-H`, `ASCII DEL`, `Escape sequence`, `TTY`).
    - `Delete key generates`: `gtk::DropDown` (`Automatic`, `Control-H`, `ASCII DEL`, `Escape sequence`, `TTY`).
    - `Encoding`: `gtk::DropDown` (`UTF-8 Unicode`, `ISO-8859-1`, `Windows-1252`, `US-ASCII`).
    - `Ambiguous-width characters`: `gtk::DropDown` (`Narrow`, `Wide`).
- **Step 2 (Badge Tab):**
  - Grid with right-aligned labels matching `screenshots/Badge.png`:
    - `Badge`: Horizontal `gtk::Box` with `gtk::Entry` and `gtk::MenuButton` (token presets).
    - `Badge position`: `gtk::DropDown` (`Northwest`, `Northeast`, `Southwest`, `Southeast`).
    - `Custom font`: Horizontal `gtk::Box` with `gtk::CheckButton` and `gtk::FontButton` (sensitive only when checked).
- **Validation:** Dropdowns update profile domain preferences; badge font button sensitivity toggles cleanly.

---

### 8. [x] Advanced Tab Implementation & Dialogs (`TASK-0010-8`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1:** Build Advanced tab layout matching `screenshots/Advanced.png`:
  - Section 1: `Notify New Activity`:
    - Bold header + description label.
    - Grid: `Enable by default` CheckButton; `Threshold for continuous silence` SpinButton + `(seconds)`.
  - Section 2: `Custom Links`:
    - Bold header + description label.
    - `[ Edit ]` button opening custom hyperlinks manager dialog.
  - Section 3: `Automatic Profile Switching`:
    - Bold header + description label.
    - Scrolled framed ListBox for rules (`Match` header).
    - Vertical button box: `[ Add ]`, `[ Edit ]`, `[ Delete ]`.
- **Step 2:** Implement Modal Dialogs:
  - Automatic Profile Switching Rule Dialog: Modal dialog with `Hostname` and `Directory` entries, enforcing the colon format requirement.
  - Custom Links Dialog: Modal dialog listing `custom_hyperlinks` with Add/Edit/Delete actions.
- **Validation:** Verify silence settings persist; verify rule list displays and Add/Delete dialogs work.

---

### 9. [x] Comprehensive Verification, Regression Testing & Architecture Update (`TASK-0010-9`)
- **Target Files:** `tests/test_phase10_profile_ui_parity.rs`, `docs/architecture/overview.md`
- **Step 1:** Run `cargo test` across all unit and integration test suites:
  - Full repo test suite must pass with 0 failures.
  - Integration test `test_phase10_profile_ui_parity` must pass 100%.
- **Step 2:** Update living architecture document:
  - Update `docs/architecture/overview.md` version to 0.10.0.
  - Add Phase 10 Profile Preferences UI Parity architectural section.
- **Validation:** `cargo test` succeeds with zero errors.
