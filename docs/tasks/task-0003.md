# Task Execution Checklist: Phase 3 — Wayland Quake, OSC CWD, DND & Interactive Preferences

- **Task ID:** `TASK-0003`
- **Spec Reference:** `docs/specs/spec-0003-wayland-quake-osc-dnd-2026-09-15.md`
- **Living Architecture Reference:** `docs/architecture/overview.md`
- **Workflow Level:** Medium/Large
- **Status:** Completed & Verified

---

## Allowed File Manifest

### Allowed New Files
- `src/model/config.rs` — Pure Rust `AppConfig`, defaults, and JSON serde persistence.
- `src/ui/quake.rs` — `TilixQuakeWindow` drop-down presentation and visibility toggling.
- `src/ui/preferences.rs` — `TilixPreferencesWindow` (`AdwPreferencesWindow`) and reactive profile binding.
- `src/ui/notifications.rs` — `NotificationService` emitting `gio::Notification` on bell and process completion.
- `src/ui/dnd.rs` — DND helpers for `GtkDragSource` and `GtkDropTarget`.
- `tests/test_phase3_domain.rs` — Headless integration test suite for Phase 3 domain features.

### Allowed Modified Files
- `src/model/mod.rs` — Export `config` module.
- `src/model/layout.rs` — Implement `swap_panes` method on `LayoutTree`.
- `src/pty/shell.rs` — Implement `parse_osc7_uri` helper.
- `src/pty/mod.rs` — Re-export `parse_osc7_uri`.
- `src/ui/mod.rs` — Re-export `quake`, `preferences`, `notifications`, and `dnd`.
- `src/ui/terminal_pane.rs` — Wire OSC 7 CWD changes, bell notifications, and DND source/target.
- `src/ui/session_view.rs` — Pass CWD on pane splits, handle pane swap events.
- `src/ui/window.rs` — Wire tab detachment (`connect_create_window`), `win.preferences` action, and CWD inheritance for `new-tab`.
- `src/app.rs` — Add `HANDLES_COMMAND_LINE`, CLI parser, and D-Bus IPC handler.
- `src/main.rs` — Update main entry point to pass CLI arguments to application.
- `docs/architecture/overview.md` — Incorporate Phase 3 living architecture updates.

### Forbidden Files
- Files outside `/playground/tilix`.
- Overwriting historical records (`result-0001.md`, `result-0002.md`, `spec-0001.md`, `spec-0002.md`, `task-0001.md`, `task-0002.md`).
- Adding unapproved third-party dependencies to `Cargo.toml`.

---

## Sequenced Subtasks

### 1. OSC 7 CWD Extraction & Headless URI Parsing (`TASK-0003-1`)
- **Step 1 (TDD Test-First):** In `src/pty/shell.rs`, write failing tests:
  - `test_parse_osc7_uri_standard`
  - `test_parse_osc7_uri_with_localhost`
  - `test_parse_osc7_uri_with_percent_encoding`
  - `test_parse_osc7_uri_invalid_scheme`
- **Step 2 (Implementation):**
  - Implement `parse_osc7_uri(uri: &str) -> Option<PathBuf>` in `src/pty/shell.rs`.
  - Re-export in `src/pty/mod.rs`.
- **Validation:** `cargo test --lib pty::shell` passes 100%.

### 2. LayoutTree Pane Swapping in Pure Domain Model (`TASK-0003-2`)
- **Step 1 (TDD Test-First):** In `src/model/layout.rs`, write failing tests:
  - `test_swap_panes_adjacent_leaves`
  - `test_swap_panes_nested_leaves`
  - `test_swap_panes_same_pane_noop`
  - `test_swap_panes_nonexistent_returns_err`
- **Step 2 (Implementation):**
  - Implement `swap_panes(&mut self, a: PaneId, b: PaneId) -> Result<(), LayoutError>` in `src/model/layout.rs`.
- **Validation:** `cargo test --lib model::layout` passes 100%.

### 3. Application Configuration & Persistence Model (`TASK-0003-3`)
- **Step 1 (TDD Test-First):** Create `src/model/config.rs` with failing tests:
  - `test_app_config_default_values`
  - `test_app_config_serialize_deserialize_json`
  - `test_app_config_roundtrip_file_io`
- **Step 2 (Implementation):**
  - Define `AppConfig` struct with `Profile`, quake settings, and notification settings.
  - Implement `load()`, `save()`, `to_json()`, and `from_json()`.
  - Re-export in `src/model/mod.rs`.
- **Validation:** `cargo test --lib model::config` passes 100%.

### 4. Headless CLI Argument Parsing & D-Bus IPC Routing (`TASK-0003-4`)
- **Step 1 (TDD Test-First):** In `src/app.rs`, write failing tests:
  - `test_parse_cli_args_empty_defaults_to_new_window`
  - `test_parse_cli_args_quake`
  - `test_parse_cli_args_quake_toggle`
  - `test_parse_cli_args_preferences`
- **Step 2 (Implementation):**
  - Define `CliAction` enum and implement `parse_cli_args`.
  - Configure `adw::Application` with `gio::ApplicationFlags::HANDLES_COMMAND_LINE`.
  - Connect `app.connect_command_line` to route actions across instances over D-Bus.
- **Validation:** `cargo test --lib app` passes 100%.

### 5. CWD Preservation Across Splits & Tabs (`TASK-0003-5`)
- In `src/ui/terminal_pane.rs`:
  - Accept `initial_directory: Option<&Path>` in `TerminalPane::new`.
  - Connect `vte.connect_current_directory_uri_changed`, parse URI, and cache in `current_directory: Rc<RefCell<Option<PathBuf>>>`.
  - Expose `pub fn current_directory(&self) -> Option<PathBuf>`.
- In `src/ui/session_view.rs`:
  - Update `split_active` and `split_pane` to retrieve the target pane's current directory and pass it to the new pane.
  - Expose `active_current_directory(&self) -> Option<PathBuf>`.
- In `src/ui/window.rs`:
  - In `win.new-tab`, retrieve the active session's active directory and initialize the new tab's session in that directory.
- **Validation:** `cargo check --all-targets` succeeds.

### 6. Quake Drop-Down Window Presentation (`TASK-0003-6`)
- Create `src/ui/quake.rs`:
  - Implement `TilixQuakeWindow` wrapping `adw::ApplicationWindow`.
  - Style with `.quake-window`, set default width 100% and top alignment.
  - Implement `toggle_visibility()` (hide if visible and active; present if hidden).
  - Embed `SessionView` inside `TilixQuakeWindow`.
- In `src/app.rs`:
  - Retain `quake_window: Rc<RefCell<Option<TilixQuakeWindow>>>`.
  - When `CliAction::QuakeToggle` arrives, invoke `toggle_visibility()`.
- **Validation:** `cargo check --all-targets` succeeds.

### 7. Desktop Notifications via GIO (`TASK-0003-7`)
- Create `src/ui/notifications.rs`:
  - Implement `NotificationService::notify_bell` and `notify_process_exit`.
- In `src/ui/terminal_pane.rs`:
  - Connect `terminal.connect_bell` and forward event.
  - Connect `terminal.connect_child_exited` and forward exit status.
- In `src/ui/window.rs` / `SessionView`:
  - If target pane does not have focus, dispatch desktop notification via `NotificationService`.
- **Validation:** `cargo check --all-targets` succeeds.

### 8. Wayland DND Tab Detaching & Pane Swapping (`TASK-0003-8`)
- In `src/ui/window.rs`:
  - Connect `tab_view.connect_create_window` to instantiate a new `TilixWindow` without initial tabs.
  - Connect `tab_view.connect_page_attached` and `tab_view.connect_page_detached` to dynamically track session maps across windows.
- In `src/ui/dnd.rs` & `src/ui/terminal_pane.rs`:
  - Attach `GtkDragSource` to `TerminalPane` header transmitting `PaneId` as `u64`.
  - Attach `GtkDropTarget` to `TerminalPane` widget receiving `u64`.
  - When dropped, notify `SessionView::swap_panes(src, dest)`.
- In `src/ui/session_view.rs`:
  - Implement `swap_panes`: updates domain `LayoutTree::swap_panes`, updates `panes` mapping, and rebuilds projection without restarting shells.
- **Validation:** `cargo check --all-targets` succeeds.

### 9. Interactive AdwPreferencesWindow & Reactive Profile Binding (`TASK-0003-9`)
- Create `src/ui/preferences.rs`:
  - Build `AdwPreferencesWindow` with Appearance page:
    - Color Scheme combo row.
    - Cursor Shape combo row.
    - Cursor Blink combo row.
    - Font selection row.
  - Connect callbacks to update active `Profile` and save to `AppConfig`.
- In `src/ui/window.rs`:
  - Register action `win.preferences` (`<Primary>comma`).
  - Wire callback to broadcast profile update across all sessions in all open windows via `apply_profile`.
- **Validation:** `cargo check --all-targets` succeeds.

### 10. End-to-End Build & Verification Gate (`TASK-0003-10`)
- Execute full test suite:
  ```bash
  cargo check --all-targets
  cargo test
  cargo clippy --all-targets -- -D warnings
  cargo build
  cargo build --release
  ```
- Verify zero warnings, zero clippy violations, and 100% passing tests.
