# Task Execution Checklist: Phase 9 — Complete Tilix Profile Customization Subsystem

- **Task ID:** `TASK-0009`
- **Spec Reference:** [`docs/specs/spec-0009-profile-customization-complete-2026-09-17.md`](file:///playground/tilix/docs/specs/spec-0009-profile-customization-complete-2026-09-17.md)
- **Living Architecture Reference:** [`docs/architecture/overview.md`](file:///playground/tilix/docs/architecture/overview.md)
- **Workflow Level:** Medium/Large
- **Status:** Complete

---

## Allowed File Manifest

### Allowed Modified Files
- `src/model/profile.rs` — Define extended Profile fields, preference enums, token expansion engine, and automatic switch rule matching.
- `src/model/config.rs` — Implement multi-profile management (`profiles: Vec<Profile>`, `default_profile_id: String`), CRUD methods, deletion safeguards, and backwards compatibility.
- `src/model/mod.rs` — Re-export new profile enums, structs, and token expansion functions.
- `src/pty/shell.rs` — Add `format_shell_argv0` (login shell) and `build_spawn_args` (custom command).
- `src/ui/terminal_pane.rs` — Add scrollbar, badge overlay, margin line, dynamic profile application for all VTE properties, exit action handling, and automatic profile switching on title/directory change.
- `src/ui/preferences.rs` — Build the comprehensive Libadwaita Profiles page with 7 tabs and profile management header.
- `src/ui/session_view.rs` — Connect per-pane profile tracking and switching.
- `src/ui/window.rs` — Add CSS for badge/margin, global profile synchronization, and profile menu integration.
- `docs/architecture/overview.md` — Bump architecture version to 0.9.0 and document Phase 9 subsystem.

### Allowed New Files
- `tests/test_phase9_profile.rs` — Integration test suite for Phase 9 covering multi-profile CRUD, legacy serialization, token expansion, shell arguments, badge overlay, and UI components.

### Forbidden Files
- Modifying production files outside the allowed list.
- Modifying or overwriting historical documents:
  - `docs/specs/spec-0001-rust-tilix-foundation-2026-09-15.md` through `docs/specs/spec-0008-pane-dnd-docking-detach-2026-09-17.md`
  - `docs/tasks/task-0001.md` through `docs/tasks/task-0008.md`
  - `docs/results/result-0001.md` through `docs/results/result-0008.md`
- Adding unapproved third-party dependencies to `Cargo.toml`.

---

## Sequenced Subtasks

### 1. Domain Model: Extended Profile Schema, Enums & Token Engine (`TASK-0009-1`)
- **Target Files:** `src/model/profile.rs`, `src/model/mod.rs`
- **Step 1 (TDD Test-First):** In `src/model/profile.rs`, add failing tests:
  - `test_profile_extended_default_values`: Asserts default values for all new fields across all 7 tabs.
  - `test_profile_serde_roundtrip`: Verifies complete JSON serialization and deserialization of a fully customized profile.
  - `test_profile_serde_backwards_compatibility`: Deserializes minimal legacy Profile JSON (only 7 fields) and asserts new fields take default values without error.
  - `test_expand_title_tokens`: Tests `${id}`, `${title}`, `${profile}`, `${directory}`, `${appName}` expansion.
  - `test_expand_badge_tokens`: Tests badge token expansion.
  - `test_automatic_switch_rule_matching`: Asserts hostname and directory pattern matching against current context.
- **Step 2 (Implementation):**
  - Define enums: `EraseBindingPreference`, `TextBlinkModePreference`, `TerminalBellPreference`, `ExitActionPreference`, `CjkWidthPreference`, `BadgePosition`, `TriggerAction`.
  - Implement conversion methods from preference enums to `vte::EraseBinding` and `vte::TextBlinkMode`.
  - Define rule structs: `ProfileSwitchRule`, `CustomHyperlinkRule`, `TriggerRule`.
  - Update `Profile` struct with fields for all 7 tabs with `#[serde(default)]`.
  - Implement `expand_title_format` and `expand_badge_format`.
  - Implement `ProfileSwitchRule::matches`.
  - Re-export new symbols in `src/model/mod.rs`.
- **Validation:** `cargo test --lib model::profile` passes 100%.

### 2. Multi-Profile Management in `AppConfig` (`TASK-0009-2`)
- **Target Files:** `src/model/config.rs`
- **Step 1 (TDD Test-First):** In `src/model/config.rs`, add failing tests:
  - `test_app_config_profiles_defaults`: Asserts `profiles` has 1 default profile and `default_profile_id == "default"`.
  - `test_app_config_add_profile`: Asserts adding a profile assigns unique ID and stores in `profiles`.
  - `test_app_config_duplicate_profile`: Asserts cloning a profile preserves settings with a new ID and `(Copy)` suffix.
  - `test_app_config_delete_profile`: Asserts deleting a non-default profile succeeds.
  - `test_app_config_delete_default_profile_reassigns_default`: Asserts deleting the default profile switches default to first remaining profile.
  - `test_app_config_cannot_delete_last_profile`: Asserts deleting the sole profile returns `Err(ProfileError::CannotDeleteLastProfile)`.
  - `test_app_config_set_default_profile`: Asserts setting default updates `default_profile_id` and `default_profile`.
  - `test_app_config_legacy_deserialization_populates_profiles`: Deserializes JSON without `profiles` field, verifying `profiles` is populated with `default_profile`.
- **Step 2 (Implementation):**
  - Define `ProfileError`.
  - Update `AppConfig` with `pub profiles: Vec<Profile>` and `pub default_profile_id: String`, retaining `pub default_profile: Profile`.
  - Implement `get_profile`, `get_profile_mut`, `get_default_profile`, `add_profile`, `duplicate_profile`, `delete_profile`, `set_default_profile`, and `update_profile`.
  - In `AppConfig::from_json`, ensure post-deserialization normalization for legacy schemas.
- **Validation:** `cargo test --lib model::config` passes 100%.

### 3. PTY Shell Spawning: Login Shell & Custom Command Execution (`TASK-0009-3`)
- **Target Files:** `src/pty/shell.rs`
- **Step 1 (TDD Test-First):** In `src/pty/shell.rs`, add failing tests:
  - `test_format_shell_argv0_login_shell`: Asserts `/bin/bash` with `login_shell: true` formats as `"-bash"`.
  - `test_format_shell_argv0_non_login_shell`: Asserts `/bin/bash` with `login_shell: false` formats as `"bash"`.
  - `test_build_spawn_args_default_shell`: Asserts spawn args for default shell return detected shell and appropriate argv.
  - `test_build_spawn_args_custom_command`: Asserts spawn args for custom command return `"/bin/sh"` with `["-c", custom_command]`.
- **Step 2 (Implementation):**
  - Implement `format_shell_argv0(shell_path: &str, login_shell: bool) -> String`.
  - Implement `build_spawn_args(profile: &Profile, detected_shell: &str) -> (String, Vec<String>)`.
- **Validation:** `cargo test --lib pty::shell` passes 100%.

### 4. TerminalPane Dynamic Profile Application, Scrollbar, Badge & Exit Actions (`TASK-0009-4`)
- **Target Files:** `src/ui/terminal_pane.rs`, `src/ui/window.rs`
- **Step 1 (TDD Test-First):** In `src/ui/terminal_pane.rs`, add headless GTK tests (using `run_gtk_test`):
  - `test_terminal_pane_scrollbar_toggle`: Verifies scrollbar visibility tracks `profile.show_scrollbar`.
  - `test_terminal_pane_badge_overlay`: Verifies badge label text, visibility, and non-targetable overlay properties.
  - `test_terminal_pane_apply_extended_profile`: Tests applying font scale, cursor shape/blink, erase bindings, CJK width, and color transparency.
  - `test_terminal_pane_exit_action_hold`: Verifies child exit under `ExitActionPreference::Hold` does not trigger close callbacks and updates title.
- **Step 2 (Implementation):**
  - In `TerminalPane::new`:
    - Wrap `vte::Terminal` and a new `gtk::Scrollbar` in a horizontal box.
    - Add `badge_label: gtk::Label` with CSS `.terminal-badge` and `set_can_target(false)` to `self.overlay`.
    - Add `margin_line: gtk::Box` with CSS `.terminal-margin-line` and `set_can_target(false)` to `self.overlay`.
  - In `TerminalPane::apply_profile`:
    - Apply `show_scrollbar` to `self.scrollbar`.
    - Apply `cell_width_scale` and `cell_height_scale`.
    - Apply `text_blink_mode`, `allow_bold`, `bold_is_bright`.
    - Apply `bold_color`, `cursor_colors`, `highlight_colors`, and alpha transparency to background.
    - Apply `backspace_binding` and `delete_binding`.
    - Apply `cjk_utf8_ambiguous_width`.
    - Apply `select_by_word_chars`.
    - Apply `scroll_on_output`, `scroll_on_keystroke`, `scrollback_lines`.
    - Format and set terminal title with `profile.terminal_title`.
    - Format and render badge with `profile.badge_text` and `badge_position`.
  - Implement exit action handling in `connect_child_exited` for `Close`, `Restart`, and `Hold`.
  - In `src/ui/window.rs`, define CSS styles for `.terminal-badge` and `.terminal-margin-line`.
- **Validation:** `cargo test --lib ui::terminal_pane` passes 100%.

### 5. Automatic Profile Switching Engine (`TASK-0009-5`)
- **Target Files:** `src/ui/terminal_pane.rs`, `src/ui/session_view.rs`
- **Step 1 (TDD Test-First):** In `src/model/profile.rs` / `src/ui/terminal_pane.rs`:
  - `test_automatic_profile_switching_eval`: Verifies matching rules against hostname and directory returns target `profile_id`.
- **Step 2 (Implementation):**
  - In `TerminalPane`:
    - On OSC 7 directory update or terminal title update, evaluate active profile's `automatic_switch` rules against current hostname and directory.
    - If a matching rule specifies a different profile ID, load that profile and invoke `apply_profile`.
- **Validation:** `cargo test` passes.

### 6. Multi-Profile Preferences Editor UI with 7 Tabs (`TASK-0009-6`)
- **Target Files:** `src/ui/preferences.rs`
- **Step 1 (Implementation):**
  - Create a dedicated "Profiles" `adw::PreferencesPage` in `TilixPreferencesWindow`:
    - Top profile selector group:
      - Dropdown displaying all profile names.
      - "New Profile" button.
      - "Duplicate Profile" button.
      - "Delete Profile" button (disabled if `profiles.len() <= 1`).
      - "Set as Default" button (indicators for default profile).
    - 7 tab groups for selected profile:
      1. **General:** Visible name, Title format string, Initial cols/rows, Cell width/height scales (1.0..2.0), Margin column, Text blink mode, Allow bold, Rewrap on resize, Font (use system font switch + font entry), Word chars, Cursor shape, Cursor blink, Terminal bell.
      2. **Command:** Login shell switch, Custom command switch + entry, Exit action combo.
      3. **Color:** Preset dropdown, Use theme colors switch, Background transparency (0..100%), Dim unfocused transparency (0..100%), Bold color override + picker, Bold is bright, Cursor colors override + pickers, Highlight colors override + pickers, 16 ANSI color palette editor.
      4. **Scrolling:** Show scrollbar, Scroll on output, Scroll on keystroke, Unlimited scrollback, Scrollback lines spin.
      5. **Compatibility:** Backspace binding combo, Delete binding combo, Encoding combo, Ambiguous CJK width combo.
      6. **Badge:** Badge text entry, Badge position combo, Custom badge color + picker, Badge font switch + entry.
      7. **Advanced:** Automatic switch rule list (Add/Delete), Custom hyperlinks rule list, Triggers rule list, Silence notification switch + threshold spin.
  - Wire reactive live updates: editing any setting immediately saves to `AppConfig` and invokes `on_profile_changed` to update running terminals.
- **Validation:** `cargo test --lib ui::preferences` passes 100%.

### 7. Window & Session Profile Integration (`TASK-0009-7`)
- **Target Files:** `src/ui/session_view.rs`, `src/ui/window.rs`
- **Step 1 (Implementation):**
  - Allow switching profile for an individual pane in `SessionView` via context menu or toolbar action.
  - Connect `apply_profile_to_all_sessions` to update all active sessions when a profile is edited.
  - Update `docs/architecture/overview.md` to version 0.9.0.
- **Validation:** `cargo test` passes.

### 8. Comprehensive Integration Test Suite (`TASK-0009-8`)
- **Target Files:** `tests/test_phase9_profile.rs`
- **Step 1 (Implementation):**
  - Write integration tests covering:
    - Multi-profile CRUD lifecycle.
    - Legacy config backwards compatibility deserialization.
    - Title and badge token expansion edge cases.
    - PTY argv construction for login shell and custom command.
    - Headless GTK test of `TerminalPane` with full profile properties applied.
    - Headless GTK test of `TilixPreferencesWindow` initialization with the new Profiles page.
- **Validation:** `cargo test --test test_phase9_profile` and full `cargo test` pass with 0 warnings/failures.

---

## Stop Conditions & Gate Checks

1. **Gate 1 (Domain & CRUD):** Subtasks 1 and 2 must pass `cargo test --lib model` before proceeding to UI.
2. **Gate 2 (PTY & TerminalPane):** Subtasks 3, 4, and 5 must pass `cargo test --lib pty` and `cargo test --lib ui::terminal_pane`.
3. **Gate 3 (Full Regression):** Complete `cargo test` must pass all existing 114+ tests plus new Phase 9 tests with 0 failures.
