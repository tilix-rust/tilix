# Task Execution Checklist: Phase 16 — Dual Library Crate & Test Compilation Optimization

- **Task ID:** `TASK-0016`
- **Spec Reference:** [`docs/specs/spec-0016-library-crate-and-test-compilation-optimization-2026-09-20.md`](file:///playground/tilix/docs/specs/spec-0016-library-crate-and-test-compilation-optimization-2026-09-20.md)
- **Living Architecture Reference:** [`docs/architecture/overview.md`](file:///playground/tilix/docs/architecture/overview.md)
- **Workflow Level:** Medium/Large
- **Status:** Completed

---

## Allowed File Manifest

### Allowed Modified Files
- `Cargo.toml` — Declare explicit `[lib]` and `[[bin]]` crate targets.
- `src/main.rs` — Convert to thin entrypoint calling `tilix::app::TilixApplication`.
- `tests/test_phase2_domain.rs` — Migrate import to `use tilix::model;`.
- `tests/test_phase3_domain.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase4_packaging_polish.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase5_window_style_wide_handle.rs` — Migrate import to `use tilix::model;`.
- `tests/test_phase6_pane_toolbar.rs` — Migrate import to `use tilix::model;`.
- `tests/test_phase7_keybindings.rs` — Migrate import to `use tilix::model;`.
- `tests/test_phase8_dnd.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase9_profile.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase10_profile_ui_parity.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase11_title_options.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase12_window_geometry.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase13_zoom_and_alt_drag.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase14_osc52_and_clipboard.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `tests/test_phase15_compact_mode.rs` — Migrate import to `use tilix::{app, model, pty, ui};`.
- `docs/architecture/overview.md` — Update version to 0.16.0 and document Section 28 Dual Library Architecture.

### Allowed New Files
- `src/lib.rs` — Library root exposing `pub mod app; pub mod model; pub mod pty; pub mod ui;`.
- `docs/specs/spec-0016-library-crate-and-test-compilation-optimization-2026-09-20.md` — Master architectural specification.
- `docs/tasks/task-0016.md` — Executable task checklist.

### Forbidden Files
- Modifying internal logic in `src/model/*`, `src/pty/*`, `src/ui/*`, or `src/app.rs`.
- Modifying packaging files (`PKGBUILD`, `Makefile`, `data/*`, `build-aux/*`).
- Modifying historical documents:
  - `docs/specs/spec-0001-rust-tilix-foundation-2026-09-15.md` through `docs/specs/spec-0015-compact-mode-density-2026-09-19.md`
  - `docs/tasks/task-0001.md` through `docs/tasks/task-0015.md`
  - `docs/results/result-0001.md` through `docs/results/result-0015.md`

---

## Sequenced Subtasks

### 1. [x] Dual Library & Binary Crate Foundation (`TASK-0016-1`)
- **Target Files:** `Cargo.toml`, `src/lib.rs`, `src/main.rs`
- **Step 1:** In `Cargo.toml`, add explicit `[lib]` and `[[bin]]` sections:
  ```toml
  [lib]
  name = "tilix"
  path = "src/lib.rs"

  [[bin]]
  name = "tilix"
  path = "src/main.rs"
  ```
- **Step 2:** Create `src/lib.rs` with public module exports:
  ```rust
  pub mod app;
  pub mod model;
  pub mod pty;
  pub mod ui;
  ```
- **Step 3:** Refactor `src/main.rs` into a lean binary entrypoint:
  ```rust
  fn main() -> glib::ExitCode {
      let app = tilix::app::TilixApplication::new();
      let args: Vec<String> = std::env::args().collect();
      app.run_with_args(&args)
  }
  ```
- **Step 4:** Validate library and binary compilation:
  - Run `cargo check --lib`
  - Run `cargo check --bin tilix`
- **Validation:** Both checks pass without error.

---

### 2. [x] Integration Test Migration — Phase 2 through Phase 7 (`TASK-0016-2`)
- **Target Files:**
  - `tests/test_phase2_domain.rs`
  - `tests/test_phase3_domain.rs`
  - `tests/test_phase4_packaging_polish.rs`
  - `tests/test_phase5_window_style_wide_handle.rs`
  - `tests/test_phase6_pane_toolbar.rs`
  - `tests/test_phase7_keybindings.rs`
- **Step 1:** In `tests/test_phase2_domain.rs`:
  - Replace `#[path = "../src/model/mod.rs"] mod model;` with `use tilix::model;`.
- **Step 2:** In `tests/test_phase3_domain.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 3:** In `tests/test_phase4_packaging_polish.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 4:** In `tests/test_phase5_window_style_wide_handle.rs`:
  - Replace `#[path = "../src/model/mod.rs"] mod model;` with `use tilix::model;`.
- **Step 5:** In `tests/test_phase6_pane_toolbar.rs`:
  - Replace `#[path = "../src/model/mod.rs"] mod model;` with `use tilix::model;`.
- **Step 6:** In `tests/test_phase7_keybindings.rs`:
  - Replace `#[path = "../src/model/mod.rs"] mod model;` with `use tilix::model;`.
- **Validation:** Run:
  ```bash
  cargo test --test test_phase2_domain --test test_phase3_domain --test test_phase4_packaging_polish --test test_phase5_window_style_wide_handle --test test_phase6_pane_toolbar --test test_phase7_keybindings
  ```
  Verify all migrated tests compile against `libtilix.rlib` and pass.

---

### 3. [x] Integration Test Migration — Phase 8 through Phase 11 (`TASK-0016-3`)
- **Target Files:**
  - `tests/test_phase8_dnd.rs`
  - `tests/test_phase9_profile.rs`
  - `tests/test_phase10_profile_ui_parity.rs`
  - `tests/test_phase11_title_options.rs`
- **Step 1:** In `tests/test_phase8_dnd.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 2:** In `tests/test_phase9_profile.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 3:** In `tests/test_phase10_profile_ui_parity.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 4:** In `tests/test_phase11_title_options.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Validation:** Run:
  ```bash
  cargo test --test test_phase8_dnd --test test_phase9_profile --test test_phase10_profile_ui_parity --test test_phase11_title_options
  ```
  Verify all migrated tests compile against `libtilix.rlib` and pass.

---

### 4. [x] Integration Test Migration — Phase 12 through Phase 15 (`TASK-0016-4`)
- **Target Files:**
  - `tests/test_phase12_window_geometry.rs`
  - `tests/test_phase13_zoom_and_alt_drag.rs`
  - `tests/test_phase14_osc52_and_clipboard.rs`
  - `tests/test_phase15_compact_mode.rs`
- **Step 1:** In `tests/test_phase12_window_geometry.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 2:** In `tests/test_phase13_zoom_and_alt_drag.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 3:** In `tests/test_phase14_osc52_and_clipboard.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Step 4:** In `tests/test_phase15_compact_mode.rs`:
  - Replace `#[path = ...]` blocks for `model`, `pty`, `ui`, `app` with `use tilix::{app, model, pty, ui};`.
- **Validation:** Run:
  ```bash
  cargo test --test test_phase12_window_geometry --test test_phase13_zoom_and_alt_drag --test test_phase14_osc52_and_clipboard --test test_phase15_compact_mode
  ```
  Verify all migrated tests compile against `libtilix.rlib` and pass.

---

### 5. [x] Packaging Polish & Release Verification (`TASK-0016-5`)
- **Target Files:** `tests/test_phase4_packaging_polish.rs`
- **Step 1:** Run packaging test suite:
  ```bash
  cargo test --test test_phase4_packaging_polish
  ```
  Verify that CLI argument parsing, desktop file consistency, PKGBUILD, Makefile, and Flatpak manifest tests pass cleanly.
- **Step 2:** Test release compilation:
  ```bash
  cargo test --release --no-run
  ```
  Verify that:
  - `libtilix.rlib` is compiled once in release mode.
  - Test binaries link against `libtilix.rlib` rapidly.
  - Peak memory usage remains under 2.5 GB without memory exhaustion.
- **Validation:** Release compilation builds cleanly and packaging tests pass.

---

### 6. [x] Living Architecture & Workspace Regression Verification (`TASK-0016-6`)
- **Target Files:** `docs/architecture/overview.md`
- **Step 1:** In `docs/architecture/overview.md`:
  - Bump document version to `0.16.0 (Phase 16 Dual Library Architecture & Test Compilation Optimization)` and update date to `2026-09-20`.
  - Add Section 28 detailing the Dual Library + Binary Architecture, compilation deduplication mechanism, memory optimization benchmarks, and test link topology.
- **Step 2:** Run complete workspace test validation:
  ```bash
  cargo test
  ```
  Verify that all 194 unit and integration tests across all test suites pass with 0 failures, 0 errors, and 0 warnings.
- **Validation:** 100% pass rate on `cargo test`.
